What do I want to do in the next commit?

- Create a function with `@function_tool` decorator to be called
- Update the system prompt and ensure the function are being called

(Done)

Next one:

- Provide UserData class to store the user data during a session
- Provide sample class to represent the data that can be changed during a session
- Update the system prompt and ensure the UserData can be changed based on the instruction

(Done)

Next one:

- Create a method and register RPC method
- Ensure the RPC can be connected with the front-end

(In Progress)
